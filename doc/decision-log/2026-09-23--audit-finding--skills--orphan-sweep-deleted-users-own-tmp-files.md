# The orphan sweep deleted the user's own *.tmp files; sweeps now match only this tool's own temp names

**Front:** Round 37: re-verification of the P8 fix (code read during the round-close review) | **Severity:** High | **Resolution:** Escalated | **Round:** 37

Found by reading 901c201's own fix before closing the round: install/remove --target project ran cleanup_orphaned_temp_files over the whole target directory -- the user's repository root -- deleting any *.tmp older than 30s anywhere under it, including .git/ and build output; --target global swept all of ~/.claude/skills, including other skills' folders. Red: 5 cases deleted against HEAD, all passing against 901c201~1. Invariant: a sweep may only remove a temp file this tool could have created. Search: grep -rn for .tmp, mkstemp, NamedTemporaryFile and uuid4 across src/ -- atomic_write_bytes and atomic_write_chunks are the only temp creators, both naming it <target name>.<uuid4 hex>.tmp, unchanged since the module's first commit (so no legacy orphan shape is newly excluded). The project owner chose between exact-name matching, sweeping only tool-owned folders, and reverting the sweep, and extended the fix to the core: cleanup_orphaned_temp_files (folderadr, 8 commands) now matches only that exact shape, and a new cleanup_orphaned_temp_files_for(paths) scopes adrpy-skills to the exact files each call writes, non-recursively (4388240). Adversarial controls: a near-miss name (notes suffix, 31 hex, uppercase hex, a different target's name) next to a written file survives; a real orphan is still removed. 11 existing tests that used names atomic_write can never produce were rewritten with realistic ones. Full suite 1193 passed, 3 skipped.
