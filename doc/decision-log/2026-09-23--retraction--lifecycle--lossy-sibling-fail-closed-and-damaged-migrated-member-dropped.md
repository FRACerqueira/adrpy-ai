# family scans no longer fail closed on a lossy-decoded sibling, and a damaged migrated header is no longer a member

Retracts item (2) of 2026-09-21--audit-finding--security--round-25-lock-dos-and-family-lossy-decode-bypass-closed (family-scan-unreliable-encoding, exclude_from_encoding_check), under the rule recorded in invalid-files-are-left-out-of-family-rules (b4d6d4c). The harm Round 25 reproduced (a corrupted Superseded sibling letting version create a duplicate family member) is now an accepted, visible limit: the sibling is reported in warnings instead of failing the command. Items (1) and (3) of that entry stand. Also obsolete: the pending sweep of lossy-decode consumers in the Round 24 entry round-24-title-separator-only-collision-closed, and the Round 27 re-verification that every family guard passes exclude_from_encoding_check. Also dropped: counts_as_family_member, which counted a migrated header whose later rows fail to parse as a family member to match the reference tool -- a fidelity choice recorded only in a test, never in this log. migration-scan-unreliable-encoding was removed too: a lossy byte that breaks a tool header now leaves its shape and is refused as damaged.
