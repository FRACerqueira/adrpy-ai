# a predecessor could end up with two live successors through tool commands alone; only the latest member is now alive

**Front:** Round 40: state-consistency front | **Severity:** High | **Resolution:** Escalated | **Round:** 40

Round 40's state-consistency front reproduced, with tool commands on valid files only, a predecessor left with two live successors, through four entries: undo then approve of a rejected successor after another took over; version or revise off a rejected successor (a version carries no --NNN suffix, so supersede did not see it); reject reverting the predecessor while another member of the successor's family was alive; approve of a successor whose predecessor pointed elsewhere. The owner's rules, fixed in 5b3b2a0: only a family's latest member is alive -- a newer version locks every member of an older version, a newer revision the older revisions of its version, unless the newer ones are a single Rejected member, checked separately for versions and for revisions (E3a); every lifecycle command refuses a locked member with not-latest-version, through one check (locking_member). A rejected successor is the end of its line, and so is its whole family (E2a): undo, version and revise refuse with rejected-successor-is-final. Accepted (E1a): once a successor has moved on to a newer version or revision that locks its suffixed member, the supersede is settled and the predecessor no longer comes back. Red/green for each entry, with positive controls (a single Rejected newer member leaves the older one live; a Rejected decision that is not a successor can still be undone).
