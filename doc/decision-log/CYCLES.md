# Decision log cycles

A **cycle** groups a range of `Round` numbers (see `INDEX.md`) under a
human-friendly name, for narrative/retrospective reference only. It is
never a field on individual decision-log entries -- only `Round` (a
single, project-wide, ever-increasing integer) lives there. This file is
the only place a cycle's name is ever recorded.

## The naming rule

1. **Derive the name from the cycle's own dominant theme**, grounded in
   the real content of its rounds (the `scope`/`Front` of its
   `audit-finding`/`doc-drift` entries, and any ADR born or touched
   during it) -- never from a sequence number alone. State which entries
   support the name, so it's checkable, not a guess.
2. **Name the outcome, not the process.** Test: could someone with zero
   context read the name and guess roughly what got safer/better? "Round
   1-10" or "Initial Audit Push" fail this test (they describe that a
   process happened). "Concurrency & Data-Integrity Hardening" passes
   (it says what changed).
3. **Short, and immutable once written here.** A correction is a new
   note pointing at the old one, never an edit in place -- same
   discipline as decision-log entries themselves. Since a cycle name
   lives in exactly one place (this file), a correction only ever touches
   one row, never a batch of entries.
4. **Named in hindsight, never planned in advance.** A cycle's own
   dominant theme isn't knowable until it's over -- naming one before it
   closes would mean guessing at content that doesn't exist yet.
5. **Closed by the same signal the `pre-release-audit` skill already
   uses to end an audit**: the project owner says it's done/paused, not
   a fixed round count or calendar cadence decided in advance. No second
   mechanism invented for this. If a new round is proposed after a long,
   ambiguous gap with no explicit close ever declared, ask whether it
   continues the current (still-open, unnamed) cycle or starts a new one
   -- never assume either way.
6. **Always proposed, never decided unilaterally by the agent** -- same
   "ask before writing" gate as every other decision-log write. A cycle
   can stay open and unnamed indefinitely; naming isn't mandatory until
   someone wants to reference the whole span as one thing.

## Cycles

| Rounds | Dates | Name | Notes |
|---|---|---|---|
| 1-10 | 2026-09-15 to 2026-09-18 | **Hardening de Concorrência e Integridade de Dados** | Closed (audit explicitly paused after round 10). Supporting evidence (rule 1): of the 12 High-severity `audit-finding` entries across this cycle, 8 are directly about concurrency/data-integrity -- lost updates and orphaned successors under concurrent access (`second-corroboration-pass-confirms-migrate-and-config-lock-gaps`, round 4), stale-config-after-lock races (`folderadr-freshness-validated-after-every-lock-acquire`, round 6), lock-loss misclassification (`verify-still-held-treats-a-read-failure-as-lock-lost`, round 6), family-guard corruption (`supersede-and-reject-family-guard-and-predecessor-selection-fixed`, round 7), an unsafe write-before-dependency-check ordering (`folderadr-new-folder-created-before-config-commits`, round 7, itself a `Retraction`), and unreadable-subdirectory-masked corruption (`fail-closed-on-unreadable-subdirectory-for-safety-decisions`, round 8, plus its two round-9 test-coverage follow-ups). This is also the cycle in which `doc/adr/ADR001V01-...md` (Domain: concurrency) was born. |
| 11 | 2026-09-18 | **Precisão de Contrato e Cobertura do Install-Config** | Closed (single-round cycle, explicitly closed and named by the project owner). Supporting evidence (rule 1): 12 `audit-finding` entries total, dominated by Usability (7, including all 3 of the round's Medium findings -- `install-config--bare-read-omitted-updated-fields-key`, `install-config--seed-plus-field-flag-misreports-updated-fields`, `migrate--no-file-is-touched-claim-was-false-after-persist-back`) and Test-Adequacy (3, all zero-prior-coverage branches closed via mutation-confirmed new tests -- `install-config--seed-schema-validation-had-zero-test-coverage`, `migrate--empty-fallback-pattern-branch-had-no-dedicated-test`, `tests--conftest-isolation-fixture-scope-undocumented`); 2 Stability findings were Low. Every Usability finding was the same shape: `describe()`/response-shape text not matching the real behavior of the install-level config module born in ADR002V01 -- including the one finding not scoped to that module at all (`init--default-repo-config-template-used-crlf-language-packs-use-lf`, a pre-existing "defaults to en-us" claim that wasn't literally true, found in passing by the same pass). Both of the round's Medium findings also carried an escalated behavior/design question, both resolved after this cycle's own rounds closed (`install-config--seed-plus-field-flag-now-raises-usage-error`, `migrate--persist-back-write-timing-confirmed-as-is`). |
| 12-16 | 2026-09-18 to 2026-09-19 | **Hardening de Validação de Entrada e Contenção de Caminho** | Closed (explicitly closed and named by the project owner; round 17, a same-day but thematically unrelated ad hoc review of `publish.yml`, is not part of this cycle). Supporting evidence (rule 1): 18 `audit-finding` entries. Started by closing the new `adrpy log` command itself -- non-atomic INDEX.md writes (`index-md-write-was-non-atomic`, round 12), a `--scope` path escape (`scope-flag-allowed-path-escape-and-parser-desync`, round 12, hardened with defense-in-depth in round 13's `scope-path-escape-lacked-defense-in-depth-and-a-containment-test`), structured-field/delimiter validation gaps (`structured-fields-skipped-delimiter-and-closed-set-validation`, round 12), and fail-closed handling of malformed classification/Round (round 13). Generalized project-wide in round 16 once the same root-cause pattern (a malformed value escaping a too-narrow check) was found reaching commands beyond `adrpy log`: `folderadr` could collapse onto the repository root itself (`folderadr-could-collapse-onto-the-repository-root`), and whitespace-only content was accepted by every free-text CLI flag until `reject_embedded_delimiter` was fixed once, at the shared function, closing every call site (`reject-embedded-delimiter-now-rejects-blank-after-strip-content`). |
| 17-28 | 2026-09-19 to 2026-09-21 | **Hardening de Superfícies de Falsificação e Exaustão de Memória** | Closed (explicitly closed and named by the project owner). Supporting evidence (rule 1): of the 12 rounds in this span, 7 (22-28) are Security audit rounds, every one carrying at least one High-severity finding -- by far the cycle's dominant theme, both by round-count and by severity concentration. Two sub-threads: field/filename forgery, closed rounds 22-24 (`round-22-status-label-marker-forgery-closed`, `round-23-title-filename-validation-gaps-closed` and its sibling `round-23-revise-scope-domain-validation-gap-closed`, `round-24-title-separator-only-collision-closed`), and memory-exhaustion/unbounded-read DoS, closed rounds 25-28 (`round-25-lock-dos-and-family-lossy-decode-bypass-closed`, `round-26-explore-unbounded-read-closed`, `round-27-quadratic-and-unbounded-reads-closed` and its siblings `round-27-parsing-grammar-findings-closed`/`round-27-empty-title-bypass-closed`, `round-28-unbounded-reads-closed-via-streaming` and its sibling `round-28-approve-reject-undo-defensive-revalidation-closed`). This is the cycle in which `doc/adr/ADR006V01-...md` (streaming I/O, implemented) was born and Accepted, and in which `doc/adr/ADR005V01-...md` (a central FailureCodes/Constants registry) was Accepted with implementation deferred. Rounds 17-21 (a CI/publish.yml hardening pass, a first cross-platform CI run's own findings, and Usability/Test-Adequacy/Stability doc-drift closure) share the same uninterrupted span but are not this cycle's dominant theme. |
| 29-31 | 2026-09-21 | **Hardening Multi-Camada da Guarda de Contenção folderadr/folderlog** | Closed (explicitly closed and named by the project owner). Supporting evidence (rule 1): of the 5 `audit-finding` entries in this span, the 3 Security entries all harden the SAME guard (the `config-folderadr-folderlog-overlap` schema check born alongside ADR007V01) against three successively narrower bypass vectors -- a raw-string comparison missing a `../` traversal, a backslash-separated nesting, and a bare case difference (`round-29-folderadr-folderlog-overlap-guard-bypass-closed`, High, Direct), a real Windows junction/symlink the schema-time check is structurally unable to see, closed by a new real-filesystem-resolution guard (`round-30-folderadr-folderlog-junction-symlink-alias-closed`, High, Escalated -- the round that revised ADR007V01 to note the guard is "necessary, not sufficient"), and a check-then-use TOCTOU window in that second guard itself, reproduced live at a 76% win rate and closed by a pre-commit re-verification (`round-31-folderadr-folderlog-toctou-narrowed`, Medium, Escalated -- ADR007V01 revised again). The remaining 2 entries (round 29's Usability doc-drift closure, `round-29-pre-existing-doc-drift-closed`, and its combined Test-Adequacy/Release-Readiness administrative closure, `round-29-too-long-codes-parity-test-changelog-branch-protection-closed`) share the same span but are not this cycle's dominant theme. |
| 32-39 | 2026-09-21 to 2026-09-23 | **Hardening do adrpy-skills e da Recuperabilidade de Escritas Múltiplas** | Closed (explicitly closed and named by the project owner). Supporting evidence (rule 1): 40 of the 57 `audit-finding`/`doc-drift` entries in this span are `skills` -- the `adrpy-skills` subsystem born with ADR009V01 and hardened from round 33 to 39: drift markers and hashing (`agentsmd-malformed-block-detection`, round 33), link containment, BOM ownership (round 38), and ownership proved by the marker hash (`agentsmd-block-ownership-proved-by-the-marker-hash`, round 39). The second thread is multi-write recoverability in `cli`/`core`, rounds 37-39: supersede writing the successor first and resuming explicitly (`supersede-writes-successor-first-and-resumes-on-retry`, `supersede-resume-requires-explicit-flag-never-guesses`), reject reverting the predecessor first and converging on retry (`reject-and-supersede-recovery-converge-and-name-working-steps`). ADR010V01 (failure `detail` on stdout) was born in round 38. Round 39 closed the cycle by simplifying: one rule for invalid decision files (`invalid-files-are-left-out-of-family-rules`), with two retractions of earlier fail-closed patterns. Scores per round: 3, 26, 14, 10, 15, 51, 67, 11 -- round 39 the first without a High since round 32, though its 5 entries each bundle several findings. |
