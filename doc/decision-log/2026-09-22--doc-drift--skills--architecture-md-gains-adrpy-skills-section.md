# doc/architecture.md gains a full adrpy-skills section, plus 5 unrelated pre-existing drift findings on the same page, plus hashing.py's own docstring fixed

**Front:** Round 36: doc-drift front | **Severity:** Low | **Resolution:** Direct | **Round:** 36

Round 36's project-wide doc-drift front found doc/architecture.md -- the project's own module-map/request-lifecycle/concurrency reference page, with Mermaid diagrams -- describes only the core adrpy CLI, with zero mention of adrpy-skills, its own entry point, module tree, or dependency on core/atomic_write.py/core/hashing.py/core/io_retry.py/core/warnings.py, despite adrpy-skills now being a real, 4-round-hardened subsystem, not a stub. Added a new 'The adrpy-skills subsystem' section: its own module-map Mermaid diagram and table, a request-flow sequence diagram (genuinely different from adrpy's own -- no repository lock, no decision-file concept), and a note on its per-command response shape (differs by command, unlike adrpy's single envelope shape; help omits warnings entirely). Also fixed, same pass, all pre-existing and unrelated to the skills gap: the module-map paragraph contradicted itself (said no core module is used by every command, then the table said Dispatch & contract's 4 modules are used by every command) and cited a stale '13 x 15' module count, corrected to 14 cli modules x 16 core modules (verified live against the source tree via ls); core/ actually has 17 files, not 16 -- the table's own 16 is correct (hashing.py is skills-only, deliberately excluded) but nothing explained the discrepancy for a reader diffing against ls core/*.py, so a note was added; 'Where to go deeper' was missing a doc/skills/ link every other doc/ subdirectory already had one for; 'warnings is always present, even when empty' was stated as a universal claim, true for every adrpy command but not for adrpy-skills' own help response, so the claim is now scoped. Also fixed src/adrpy/core/hashing.py's own module docstring, which still said the drift marker is 'trailing' -- it's 'leading', the same claim a Round 33 decision-log entry already fixed in doc/skills/README.md, but that entry's own text said no other file needed correction, missing this one.
