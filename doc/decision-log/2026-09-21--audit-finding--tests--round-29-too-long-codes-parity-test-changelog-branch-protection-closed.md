# Closed a forward-looking test-adequacy gap (_TOO_LONG_CODES structural parity) plus 2 release-readiness admin items (CHANGELOG, branch protection)

**Front:** Test-Adequacy audit (round 29, pre-release audit) + Release-Readiness audit (round 29, pre-release audit) | **Severity:** Low | **Resolution:** Direct | **Round:** 29

Round 29's Test-Adequacy pass found a forward-looking gap: _TOO_LONG_CODES's own completeness is only protected by test_every_too_long_field_raises_its_own_matching_code's hand-maintained 15-entry parametrize list, not derived from the schema/registry themselves -- a future schema field added to _HEADER_LABEL_FIELDS_MAX_40/_STATUS_LABEL_FIELDS and _TOO_LONG_CODES with no matching new parametrize entry would get zero test signal, surfacing in production as a raw KeyError instead of a clean CommandError. Closed with a new structural test (test_too_long_codes_mapping_has_exactly_one_entry_per_schema_field) that derives its own expectations from _HEADER_LABEL_FIELDS_MAX_40/_STATUS_LABEL_FIELDS/_TOO_LONG_CODES directly, not a second hand-maintained list -- mutation-verified live (removed the headerrevision entry from _TOO_LONG_CODES, confirmed the new test failed for the exact predicted reason, restored, confirmed green). Also closed 2 administrative release-readiness items from the same round's own pass: CHANGELOG.md's Unreleased section was missing the log command entirely and this session's own ADR005V01/ADR007V01/supersede --title work -- added. main had no branch protection at all (confirmed via GitHub API, 404 'not protected') despite CI genuinely running and passing on every push -- enabled required status checks (all 9 real CI job names: test x {ubuntu,windows,macos}-latest x Python {3.11,3.12,3.13}) so CI is now an enforced merge gate on main, not just an observed one. The 8-file documentation-drift finding from the same round (7 command doc pages missing failure-code sentences, installconfig.md's tagline, doc/architecture.md's ADR007V01 citation gap) is tracked separately, being fixed in parallel. Suite: 1020 passed, 3 skipped, 1 xfailed, 0 failed.
