# lifecycle, architecture and command docs corrected against the code

**Front:** Round 40: documentation-vs-code front | **Severity:** Medium | **Resolution:** Direct | **Round:** 40

Fixed in 5b3b2a0. doc/architecture.md's state diagram showed Superseded as terminal (M1), and its JSON contract put warnings beside data and claimed it was always present (M2). The revise docs presented branching off an older revision as usable when it always collided (M3, now fixed by the numbering change). doc/lifecycle.md: 'strict YYYY-MM-DD' (fromisoformat accepts more), new's --refdate omitted, 'changes nothing' (housekeeping still happens), cells 'written once', a mixed filename example, 'next free number' (gaps are not reused), a placeholder predecessor goes back to a placeholder, undo accepts any Changed value, the exact warning text, explore's partial fields on invalid files, the migrate remedy for a legacy file; the page was rewritten for the Round 40 family rules. Also: new's 'exact title' (it is normalized), revise.md's example without a revision, the four universal codes missing from the commands index, remove's --force not naming indented, list's drifted semantics, and the adulterated-header heuristic named in explore/migrate.
